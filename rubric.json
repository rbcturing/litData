{
  "upload_directory": "/tmp/files/",
  "output_directory": "/tmp/outputs/",
  "system_prompt": "You are an expert code reviewer. Analyze the provided codebase for bugs, security issues, and correctness problems. Provide actionable feedback. All files are located in /tmp/files",
  "task_prompt": "You are an expert code reviewer. Your task is to analyze the litdata codebase. Identify bugs, wrong sample counts, off-by-one errors, integer overflow in byte conversion, worker assignment imbalance, subsample argmax boundary, arithmetic errors in replay sampling, benchmark CLI validation logic, litdata stream benchmark sample count, and linspace point count in examples. Focus on: benchmarks/stream_raw_imagenet.py batch count, .github/benchmark/benchmark.py user/org validation, benchmarks/litdata/stream_imagenet.py sample accumulation, examples/sine_function_model_prediction/01-optimize.py input count, src/litdata/utilities/format.py overflow in _convert_bytes_to_int, src/litdata/processing/data_processor.py remainder distribution order, src/litdata/utilities/subsample.py argmax comparison, and src/litdata/streaming/dataset.py _replay_sampling. Write your findings to /tmp/outputs/review.md",
  "rubric": [
    {
      "criterion": "Identifies the wrong sample count in benchmarks/stream_raw_imagenet.py where total uses len(batch) (number of batch keys/tensors) instead of the actual batch size (e.g. batch[0].size(0)), so processed sample count is incorrect",
      "weight": 1
    },
    {
      "criterion": "Identifies the validation logic error in .github/benchmark/benchmark.py in LitDataBenchmark.__init__ where the first check uses 'or' (config.user is None or config.org is None) instead of 'and', making it impossible to run with only one of user or org set",
      "weight": 1
    },
    {
      "criterion": "Identifies the wrong sample count in benchmarks/litdata/stream_imagenet.py where num_samples is incremented by len(data) instead of data[0].shape[0], undercounting streamed samples",
      "weight": 1
    },
    {
      "criterion": "Identifies the off-by-one in examples/sine_function_model_prediction/01-optimize.py where np.linspace(-5, 5, 999) is used instead of 1000 points, producing 999 samples",
      "weight": 1
    },
    {
      "criterion": "Identifies the silent integer overflow bug in src/litdata/utilities/format.py in the _convert_bytes_to_int function where float multiplication is converted to int without overflow checking (line ~31), potentially causing data loss for very large cache size specifications",
      "weight": 1
    },
    {
      "criterion": "Identifies the worker assignment imbalance in src/litdata/processing/data_processor.py in the _map_items_to_workers_sequentially function where remainder distribution is changed from reverse iteration to forward iteration (line ~353), causing load imbalance when chunk counts are not evenly divisible",
      "weight": 1
    },
    {
      "criterion": "Identifies the off-by-one error in subsample logic in src/litdata/utilities/subsample.py in the subsample_filenames_and_roi function where >= is changed to > in argmax (line ~51), causing incorrect chunk selection when cumsum_sizes exactly equals item_count",
      "weight": 1
    },
    {
      "criterion": "Identifies the arithmetic error in src/litdata/streaming/dataset.py in the _replay_sampling function where divisible_num_batches_yielded is computed with - 1 (line ~759), causing off-by-one errors in worker index replay during dataloader state resumption",
      "weight": 1
    }
  ]
}
